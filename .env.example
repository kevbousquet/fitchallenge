# Clé API Anthropic pour l'analyse des repas par photo
# TODO(prod): En production, NE PAS exposer cette clé côté client.
#             Créer un backend proxy (ex. Express/Fastify) qui reçoit la photo,
#             appelle l'API Claude côté serveur, et renvoie le résultat.
VITE_ANTHROPIC_API_KEY=sk-ant-votre-cle-ici
